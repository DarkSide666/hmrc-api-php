language: php

php:
  - '7.4'
  - '8.1'
  - '8.2'

env:
  matrix:
    - COMPOSER_FLAGS="--prefer-lowest"
    - COMPOSER_FLAGS=""

before_script:
  - travis_retry composer update ${COMPOSER_FLAGS}

script:
  - composer test
